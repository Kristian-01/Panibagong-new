import 'package:flutter/material.dart';

class PrescriptionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Prescription Upload")),
      body: Center(child: Text("This is where customers upload prescriptions.")),
    );
  }
}
